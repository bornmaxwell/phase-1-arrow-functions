// divide function expression
const divide = function() {
    return 2000 / 100;
  };
  
  // square arrow function
  const square = (num) => num * num;
  
  // add arrow function
  const add = (a, b) => a + b;